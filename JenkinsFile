pipeline {
	agent any
	stages {
	    stage ('Initiate tests on BrowserStack') {
	        steps {
    			browserstack(credentialsId: "${credentials}") {
					nodejs('Node 16') {
                    	sh "npm run ${configuration}"
					}
                }
	        }
		}
	}
}